name: Working Status

on:
  push:
    branches: [ main, master ]

jobs:
  working-status:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Working Status Report
      run: |
        echo "🎉 GitHub Actions РАБОТАЮТ!"
        echo "=============================="
        echo "✅ Репозиторий: ${{ github.repository }}"
        echo "✅ Ветка: ${{ github.ref_name }}"
        echo "✅ Коммит: ${{ github.sha }}"
        echo "✅ Пользователь: ${{ github.actor }}"
        echo ""
        echo "🚀 Этот workflow запускается автоматически при пуше!"
        echo "💡 GitHub Actions настроены правильно!"
