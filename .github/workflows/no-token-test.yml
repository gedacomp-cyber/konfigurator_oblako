name: No Token Test

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  test-without-token:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        # Без токена - только публичный доступ
        token: ''

    - name: Test basic functionality
      run: |
        echo "🧪 Testing without GITHUB_TOKEN"
        echo "==============================="
        echo "Repository: ${{ github.repository }}"
        echo "Branch: ${{ github.ref_name }}"
        echo "SHA: ${{ github.sha }}"
        echo ""

    - name: Check repository info
      run: |
        echo "📊 Repository Information:"
        echo "Owner: ${{ github.repository_owner }}"
        echo "Name: ${{ github.event.repository.name }}"
        echo "Full name: ${{ github.event.repository.full_name }}"
        echo "Private: ${{ github.event.repository.private }}"
        echo ""

    - name: Test file access
      run: |
        echo "📁 Testing file access:"
        ls -la
        echo ""
        echo "Sait directory:"
        ls -la sait/
        echo ""

    - name: Success message
      run: |
        echo "✅ Workflow completed successfully!"
        echo ""
        echo "🎯 This proves that:"
        echo "   - Repository access works"
        echo "   - File operations work"
        echo "   - The issue is likely with GITHUB_TOKEN permissions"
        echo ""
        echo "🔧 Next: Check repository settings and permissions"
